        {{!-- <div class="url"><small>{{result.url}}</small></div>
        <h2 class="p-0"><a href="{{result.url}}">{{result.title}}</a></h2>
        <p>{{result.description}}</p>
        <ul>
            {{#each links as |link|}}
          <li><a href="{{link.url}}">{{link.title}}</a></li>
          {{/each}}
        </ul> --}}